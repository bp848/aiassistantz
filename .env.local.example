# ===========================================
# 環境変数サンプル（このファイルをコピーして .env.local を作成し値を設定）
# 【重要】 .env.local は絶対に Git にコミットしないこと（.gitignore で除外済み）
# ===========================================

# Gemini API Key（必須）
# https://ai.google.dev/ で取得
GEMINI_API_KEY=your_gemini_api_key_here

# Google OAuth - Workspace 連携用（IntegrationSetup で使用）
# クライアントシークレットはフロントに置かない
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id.apps.googleusercontent.com

# MCP Server URL（未設定時は Supabase MCP がデフォルト）
# VITE_MCP_SERVER_URL=https://mcp.supabase.com/mcp?project_ref=YOUR_PROJECT_REF

# Supabase（Auth / DB / Storage 用）
# Supabase Dashboard > Settings > API で確認
REACT_APP_SUPABASE_URL=https://xxxx.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your_supabase_anon_key_here
