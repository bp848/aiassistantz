# ===========================================
# 環境変数サンプル（このファイルをコピーして .env.local を作成し値を設定）
# 【重要】 .env.local は絶対に Git にコミットしないこと（.gitignore で除外済み）
# ===========================================

# Gemini API Key（必須）
# ※ Google AI Studio で発行すること。Google Cloud Console のキーは 401 になります。
# https://aistudio.google.com/apikey → Create API key（新しいプロジェクトで発行）
GEMINI_API_KEY=your_gemini_api_key_here

# Google OAuth - Workspace 連携用（IntegrationSetup で使用）
# クライアントシークレットはフロントに置かない
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id.apps.googleusercontent.com

# MCP: ブラウザは同一オリジン /api/mcp-sse に接続（CORS 回避）。未設定時は自動。
# VITE_MCP_SERVER_URL=https://your-domain.com/api/mcp-sse

# MCP プロキシ用（Vercel の環境変数）。本番で /api/mcp-sse が使う上流 URL
# MCP_UPSTREAM_URL=https://mcp.supabase.com/mcp?project_ref=YOUR_PROJECT_REF

# Supabase（Auth / DB / Storage 用）
# Supabase Dashboard > Settings > API で確認
REACT_APP_SUPABASE_URL=https://xxxx.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your_supabase_anon_key_here
